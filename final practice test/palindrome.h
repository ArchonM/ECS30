#ifndef _PALIDROME_H_
#define _PALIDROME_H_
    //this function returns true if this line of the sentence is palindrome
    //or it returns false if this line of the sentence is not palindrome
    bool IsPalindrome(char* line);
    //this function counts how many lines of the sentence are palindrome and return the integer
    int LinesWithPalindrome(char** lines, int numlines);

#endif
